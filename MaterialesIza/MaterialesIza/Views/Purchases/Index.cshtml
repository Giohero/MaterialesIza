@model IEnumerable<MaterialesIza.Data.Entities.Purchase>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.Provider.User.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Provider.User.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Provider.User.PhoneNumber)
            </th>*@
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.Provider.User.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Provider.User.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Provider.User.PhoneNumber)
            </td>*@

            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
